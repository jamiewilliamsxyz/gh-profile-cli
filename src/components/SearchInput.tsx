import React from 'react';
import {Box, Text} from 'ink';

const SearchInput = () => {
	return (
		<Box flexDirection="row" gap={2}>
			<Text>Enter a GitHub username...</Text> <Text></Text>
		</Box>
	);
};

export default SearchInput;
